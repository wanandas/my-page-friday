import { Chip, styled } from "@mui/material";

export const BadgeCustom = styled(Chip)``;
// Hover effect removed to prioritize theme/custom colors and simplify.
// If specific hover styles are needed later, they should also consider theme colors.
